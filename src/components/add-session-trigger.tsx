"use client"; import { useState } from "react"; export function AddSessionTrigger() { const [loading, setLoading] = useState(false); async function handleSubmit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); const form = new FormData(e.currentTarget); const payload = { event_name: form.get("event_name"), track_name: form.get("track_name"), vehicle: form.get("vehicle"), tire_pressure: form.get("tire_pressure"), shock_setup: form.get("shock_setup"), weather: form.get("weather"), lap_times: form.get("lap_times"), }; await fetch("/api/sessions/create", { method: "POST", headers: { "Content-Type": "application/json", }, body: JSON.stringify(payload), }); setLoading(false); } return ( <form onSubmit={handleSubmit} className="flex gap-2"> <input name="event_name" placeholder="Event" className="text-black" /> <input name="track_name" placeholder="Track" className="text-black" /> <button disabled={loading} className="px-4 py-2 bg-red-500 text-white rounded" > {loading ? "Saving..." : "Add Session"} </button> </form> ); }
